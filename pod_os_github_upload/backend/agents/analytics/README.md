# Analytics Agent

Service boundary for revenue, profit, conversion, sellers, niches, order cadence, keywords, prompts and ROI.

MVP implementation is dispatched from `backend.agents.registry.AgentRegistry.run_analytics`.
