# Approval Queue

Service boundary for human approval, rejection and publishing gates.

MVP implementation is dispatched from `backend.agents.registry.AgentRegistry.approve_product` and `reject_product`.
