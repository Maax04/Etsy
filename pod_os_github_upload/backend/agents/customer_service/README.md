# Customer Service Agent

Service boundary for drafting replies. The agent must never send customer messages automatically.

MVP implementation is dispatched from `backend.agents.registry.AgentRegistry.run_customer_service`.
